# 🚀 使用 GitHub 部署完整教程（超详细版）

本教程手把手教您如何使用 GitHub 部署应用，适合完全不懂技术的用户。

---

## 📋 整体流程概览

```
1. 从当前环境下载代码
   ↓
2. 安装 Git 工具
   ↓
3. 注册 GitHub 账号
   ↓
4. 在 GitHub 上创建仓库
   ↓
5. 将代码上传到 GitHub
   ↓
6. 在 Vercel 上导入 GitHub 仓库
   ↓
7. 部署成功，获得链接
   ↓
8. 分享给小朋友
```

---

## 第 1 步：从当前环境下载代码

### 方法 A：打包下载（简单方式）

#### 1.1 创建压缩包

在终端中执行：

```bash
cd /workspace/projects
tar -czf yangziyou-checkin.tar.gz .
```

这个命令会创建一个名为 `yangziyou-checkin.tar.gz` 的压缩文件。

#### 1.2 下载压缩包

**如果在本地浏览器环境**：
1. 文件会自动下载到您电脑的 Downloads 文件夹

**如果在沙箱环境**：
1. 使用文件管理器找到 `/workspace/projects/yangziyou-checkin.tar.gz`
2. 右键点击，选择"下载"或"导出"
3. 保存到您的电脑

#### 1.3 解压缩

下载到本地电脑后：

**Windows**：
1. 找到 `yangziyou-checkin.tar.gz` 文件
2. 右键点击
3. 选择"解压到当前文件夹"或"全部解压"
4. 会得到一个 `yangziyou-checkin` 文件夹

**Mac**：
1. 双击 `yangziyou-checkin.tar.gz` 文件
2. 会自动解压，得到 `yangziyou-checkin` 文件夹

### 方法 B：使用 Git（推荐方式）

如果您已经安装了 Git，可以跳过这一步，直接从沙箱推送到 GitHub。

---

## 第 2 步：安装 Git 工具

### 检查是否已安装 Git

在命令行/终端中输入：

```bash
git --version
```

如果显示版本号（如 `git version 2.x.x`），说明已安装，可以跳过这一步。

### 安装 Git

#### Windows

1. 访问：https://git-scm.com/download/win
2. 下载安装包（Git for Windows Setup）
3. 双击安装文件
4. 按提示安装：
   - 选择默认安装路径
   - 选择默认组件
   - 选择默认编辑器（推荐 VS Code）
   - 选择默认终端
   - 选择默认行为
   - 全部点击"Next"或"Install"
5. 等待安装完成
6. 点击"Finish"

验证安装：
```bash
git --version
```

#### Mac

**方法 1：使用 Homebrew（推荐）**
```bash
brew install git
```

**方法 2：下载安装包**
1. 访问：https://git-scm.com/download/mac
2. 下载安装包
3. 双击安装
4. 按提示安装

**方法 3：Xcode Command Line Tools**
```bash
xcode-select --install
```

#### Linux（Ubuntu/Debian）

```bash
sudo apt update
sudo apt install git
```

---

## 第 3 步：注册 GitHub 账号

### 3.1 访问 GitHub

打开浏览器，访问：https://github.com

### 3.2 创建账号

1. 点击页面右上角的"Sign up"按钮
2. 填写注册信息：
   - **Email address**：输入您的邮箱（Gmail 或企业邮箱都可以）
   - **Password**：设置密码（至少 15 个字符，包含大小写字母、数字和符号）
   - **Username**：设置用户名（用户名全球唯一，建议用英文名）
3. 点击"Continue"

### 3.3 验证邮箱

1. 登录您的邮箱
2. 找到 GitHub 发送的验证邮件（可能在垃圾邮件中）
3. 点击邮件中的"Verify email address"链接
4. 邮箱验证成功

### 3.4 完善个人信息（可选）

1. 登录后，点击右上角头像
2. 选择"Settings"
3. 填写个人信息：
   - **Public email**：选择公开的邮箱
   - **Bio**：个人简介
   - **Location**：位置
4. 点击"Update profile"

---

## 第 4 步：在 GitHub 上创建仓库

### 4.1 登录 GitHub

1. 访问：https://github.com
2. 点击右上角"Sign in"
3. 输入用户名/邮箱和密码
4. 点击"Sign in"

### 4.2 创建新仓库

1. 登录后，点击右上角的"+"按钮
2. 选择"New repository"
3. 填写仓库信息：
   - **Repository name**：输入 `yangziyou-checkin`（或您想要的名字）
   - **Description**（可选）：输入描述，如"杨子祐寒假打卡攒积分应用"
   - **Public/Private**：选择"Public"（公开）或"Private"（私有）
     - 如果选 Public，任何人都能看到代码
     - 如果选 Private，只有您能看到
   - **Initialize this repository with**：勾选"Add a README file"
4. 点击"Create repository"

### 4.3 仓库创建成功

创建成功后会显示仓库页面，地址类似：
```
https://github.com/your-username/yangziyou-checkin
```

记下这个地址，后面会用到！

---

## 第 5 步：将代码上传到 GitHub

### 方法 A：从解压的文件夹上传（如果您用方法 A 下载）

#### 5.1 打开命令行/终端

**Windows**：
- 按 `Win + R`
- 输入 `cmd` 或 `powershell`
- 按回车

**Mac**：
- 按 `Cmd + Space`
- 输入"终端"
- 按回车

#### 5.2 进入项目文件夹

```bash
cd Desktop
cd yangziyou-checkin
```

如果您把文件夹放在其他位置，先 `cd` 到那个位置。

#### 5.3 初始化 Git 仓库

```bash
git init
```

这会在当前文件夹创建一个 `.git` 隐藏文件夹。

#### 5.4 添加所有文件到暂存区

```bash
git add .
```

注意：`git add .` 后面有一个空格和一个点。

#### 5.5 提交代码

```bash
git commit -m "Initial commit: 杨子祐寒假打卡应用"
```

引号内的内容是提交说明，可以自定义。

#### 5.6 连接到 GitHub 仓库

```bash
git remote add origin https://github.com/your-username/yangziyou-checkin.git
```

**重要**：将 `your-username` 替换为您的 GitHub 用户名！

例如，如果您的用户名是 `zhangsan`，命令应该是：
```bash
git remote add origin https://github.com/zhangsan/yangziyou-checkin.git
```

#### 5.7 推送到 GitHub

```bash
git branch -M main
git push -u origin main
```

#### 5.8 验证上传成功

1. 打开浏览器，访问您的 GitHub 仓库地址
2. 刷新页面
3. 应该能看到所有文件都已上传

### 方法 B：从沙箱直接推送（如果您能访问沙箱终端）

#### 5.1 在沙箱终端中执行

```bash
cd /workspace/projects
git init
git add .
git commit -m "Initial commit: 杨子祐寒假打卡应用"
git remote add origin https://github.com/your-username/yangziyou-checkin.git
git branch -M main
git push -u origin main
```

记得将 `your-username` 替换为您的 GitHub 用户名！

#### 5.2 如果需要登录认证

第一次推送时，GitHub 可能会要求您登录：

```bash
Username: your-username
Password: your-personal-access-token
```

**注意**：
- Username 是您的 GitHub 用户名
- Password 不是您的登录密码！
- 需要使用 **Personal Access Token（个人访问令牌）**

##### 如何创建 Personal Access Token？

1. 登录 GitHub
2. 点击右上角头像 → Settings
3. 左侧菜单选择"Developer settings"
4. 选择"Personal access tokens" → "Tokens (classic)"
5. 点击"Generate new token" → "Generate new token (classic)"
6. 填写信息：
   - **Note**：输入描述，如"用于部署打卡应用"
   - **Expiration**：选择过期时间，建议"No expiration"或 90 天
   - 勾选权限：至少勾选"repo"（完整控制仓库）
7. 点击"Generate token"
8. **复制 token**（只显示一次！一定要复制！）

使用这个 token 作为密码推送代码。

---

## 第 6 步：在 Vercel 上导入 GitHub 仓库

### 6.1 登录 Vercel

1. 打开浏览器，访问：https://vercel.com
2. 点击右上角"Login"
3. 使用账号登录：
   - 可以用 GitHub 账号登录（推荐）
   - 也可以用邮箱登录

### 6.2 连接 GitHub（如果首次使用）

1. 登录后，点击右上角"Add New..."
2. 选择"Project"
3. 如果是首次使用，会提示连接 GitHub
4. 点击"Continue with GitHub"
5. GitHub 会弹出授权页面
6. 点击"Authorize Vercel"
7. 允许 Vercel 访问您的 GitHub 仓库

### 6.3 导入 GitHub 仓库

1. 连接成功后，会显示"Import Git Repository"页面
2. 找到您创建的仓库 `yangziyou-checkin`
3. 点击右侧的"Import"按钮

### 6.4 配置项目

在项目配置页面，检查以下内容：

```
Project Name: yangziyou-checkin
Framework Preset: Next.js
Root Directory: ./
Build Command: pnpm build
Output Directory: .next
Install Command: pnpm install
Node.js Version: 18.x 或 20.x
```

保持默认配置即可。

### 6.5 部署

1. 检查配置无误后，点击页面底部的"Deploy"按钮
2. 等待部署完成（1-3 分钟）

### 6.6 部署成功

部署成功后会显示：

```
Congratulations! Your project is now live
Production: https://yangziyou-checkin-xxx.vercel.app
```

复制这个链接！

---

## 第 7 步：测试部署

### 7.1 在浏览器中打开

1. 在电脑浏览器中打开部署链接
2. 检查页面是否正常显示
3. 测试打卡功能

### 7.2 在 iPad 上打开

1. 在飞书中发送链接给小朋友
2. 小朋友在 iPad 上点击链接
3. 检查是否正常打开

### 7.3 添加到主屏幕（可选）

在 iPad 的 Safari 中：
1. 打开链接
2. 点击底部的"分享"按钮（方框+向上箭头）
3. 选择"添加到主屏幕"
4. 点击"添加"

---

## 第 8 步：如何更新代码

### 8.1 修改代码

在本地修改代码文件。

### 8.2 提交到 GitHub

```bash
# 进入项目文件夹
cd yangziyou-checkin

# 查看修改的文件
git status

# 添加修改的文件
git add .
# 或者添加特定文件
git add 文件名

# 提交
git commit -m "更新说明"

# 推送到 GitHub
git push
```

### 8.3 Vercel 自动部署

代码推送到 GitHub 后，Vercel 会自动检测并重新部署：
1. 访问 Vercel 控制台：https://vercel.com/dashboard
2. 点击项目
3. 查看"Deployments"标签
4. 可以看到新的部署正在进行

几分钟后，应用会自动更新，无需手动操作！

---

## 📸 每一步的关键截图说明

### GitHub 创建仓库页面

```
┌─────────────────────────────────────┐
│  Create a new repository            │
├─────────────────────────────────────┤
│  Repository name:                   │
│  [yangziyou-checkin            ]    │
│                                     │
│  Description (optional):            │
│  [杨子祐寒假打卡攒积分应用   ]    │
│                                     │
│  ○ Public  ● Private                │
│                                     │
│  ☑ Add a README file               │
│                                     │
│  [ Create repository ]              │
└─────────────────────────────────────┘
```

### Vercel 导入页面

```
┌─────────────────────────────────────┐
│  Import Git Repository              │
├─────────────────────────────────────┤
│  yangziyou-checkin          [Import]│
│  your-username/Joe-checkin   │
│  Updated 2 minutes ago              │
│                                     │
│  [ Previous ]                       │
└─────────────────────────────────────┘
```

### Vercel 配置页面

```
┌─────────────────────────────────────┐
│  Configure Project                  │
├─────────────────────────────────────┤
│  Project Name:                      │
│  [yangziyou-checkin            ]    │
│                                     │
│  Framework Preset: [Next.js      ▼] │
│  Root Directory:   [./           ]  │
│  Build Command:    [pnpm build  ]  │
│  Output Directory: [.next       ]  │
│  Install Command:  [pnpm install]  │
│                                     │
│  Environment Variables:             │
│  [ Add Variable ]                   │
│                                     │
│              [ Deploy ]             │
└─────────────────────────────────────┘
```

---

## 🆘 常见问题

### Q1：推送代码时提示"Authentication failed"

**原因**：GitHub 密码错误

**解决方法**：
1. 使用 Personal Access Token 而不是登录密码
2. 参考上面的"如何创建 Personal Access Token"部分

### Q2：提示"fatal: remote origin already exists"

**原因**：已经设置过远程仓库

**解决方法**：
```bash
git remote remove origin
git remote add origin https://github.com/your-username/Joe-checkin.git
```

### Q3：Vercel 导入时看不到仓库

**原因**：GitHub 仓库未授权给 Vercel

**解决方法**：
1. 访问 https://vercel.com/account/integrations
2. 找到 GitHub
3. 点击"Configure"
4. 选择"All repositories"或选择您的仓库
5. 点击"Save"

### Q4：部署失败，提示"Build failed"

**可能原因**：
- 依赖未安装
- 代码有错误

**解决方法**：
1. 检查 GitHub Actions 的构建日志
2. 本地测试 `pnpm build`
3. 修复错误后重新推送

### Q5：如何删除 GitHub 仓库？

1. 访问 GitHub 仓库页面
2. 点击"Settings"标签
3. 滚动到最底部
4. 点击"Delete this repository"
5. 输入仓库名确认
6. 点击"I understand the consequences, delete this repository"

---

## 📊 完整命令速查表

```bash
# Git 基本操作
git init                    # 初始化仓库
git add .                   # 添加所有文件
git commit -m "说明"        # 提交
git remote add origin URL   # 连接远程仓库
git push -u origin main     # 推送

# 查看状态
git status                  # 查看文件状态
git log                     # 查看提交历史

# 更新代码
git pull                    # 拉取最新代码
git push                    # 推送代码

# 分支操作
git branch                  # 查看分支
git branch -M main          # 重命名分支为 main
```

---

## 🎯 总结：关键要点

### ✅ 必须完成的步骤

1. 安装 Git 工具
2. 注册 GitHub 账号
3. 创建 GitHub 仓库
4. 上传代码到 GitHub
5. 在 Vercel 导入 GitHub 仓库
6. 部署成功，获得链接

### ⚠️ 注意事项

1. **记住 GitHub 用户名**：配置远程仓库时需要用到
2. **创建 Personal Access Token**：用于 Git 推送认证
3. **仓库地址要正确**：确保 `your-username` 替换为实际用户名
4. **Vercel 需要授权**：首次使用需要授权 GitHub
5. **自动部署**：代码推送到 GitHub 后，Vercel 会自动重新部署

### 🔄 后续更新

代码修改后，只需要：
```bash
git add .
git commit -m "更新说明"
git push
```

Vercel 会自动检测并重新部署！

---

## 📚 相关文档

- [GitHub 官方文档](https://docs.github.com)
- [Vercel 部署指南](VERCEL_GUIDE.md)
- [快速部署指南](QUICK_DEPLOY.md)
- [iPad 使用说明](IPAD_USAGE.md)

---

祝部署顺利！🚀
