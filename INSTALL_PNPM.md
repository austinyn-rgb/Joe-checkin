# 🔧 安装 pnpm 和 npm 使用指南

您遇到的"pnpm不是内部或外部命令"错误，说明您的电脑上没有安装 pnpm。

本指南将教您如何安装 pnpm，以及如何使用 npm 作为替代方案。

---

## 📦 方案一：安装 pnpm（推荐）

### 方法 1：使用 npm 安装 pnpm（最简单）

#### 步骤 1：检查是否已安装 npm

在 CMD 中输入：
```cmd
npm --version
```

如果显示版本号（如 `9.x.x`），说明已经安装了 npm，可以直接进行下一步。

如果提示"不是内部或外部命令"，需要先安装 Node.js。

#### 步骤 2：安装 pnpm

在 CMD 中输入：
```cmd
npm install -g pnpm
```

按回车，等待安装完成（1-3 分钟）。

#### 步骤 3：验证安装

```cmd
pnpm --version
```

如果显示版本号（如 `9.x.x`），说明安装成功！

#### 步骤 4：安装项目依赖

进入项目文件夹：
```cmd
cd Desktop\Joe-checkin
```

安装依赖：
```cmd
pnpm install
```

### 方法 2：使用 npm 安装 Node.js（如果 npm 也没有）

#### 步骤 1：下载 Node.js

1. 访问：https://nodejs.org
2. 点击"Download"按钮（推荐 LTS 版本）
3. 下载安装包（Windows：`.msi` 文件）

#### 步骤 2：安装 Node.js

1. 双击下载的 `.msi` 安装包
2. 点击"Next"
3. 选择安装位置（使用默认位置）
4. 点击"Next"
5. 点击"Next"（选择默认组件）
6. 点击"Next"
7. 点击"Install"
8. 等待安装完成
9. 点击"Finish"

**重要**：安装完成后需要**重启 CMD** 才能生效！

#### 步骤 3：重启 CMD

关闭当前的 CMD 窗口，重新打开一个新的 CMD 窗口。

#### 步骤 4：验证安装

```cmd
node --version
npm --version
```

应该都能看到版本号。

#### 步骤 5：安装 pnpm

```cmd
npm install -g pnpm
```

### 方法 3：使用 npm 全局安装脚本

如果方法 1 不行，可以尝试使用安装脚本：

在 CMD 中输入：
```cmd
npm install -g @pnpm/exe
```

然后验证：
```cmd
pnpm --version
```

### 方法 4：使用 PowerShell 安装

如果 CMD 中安装失败，可以尝试 PowerShell：

1. 按 `Win + X`
2. 选择"Windows PowerShell"或"终端"
3. 输入：
```powershell
npm install -g pnpm
```

### 方法 5：使用 Chocolatey（高级用户）

如果您的电脑已安装 Chocolatey：

```powershell
choco install pnpm
```

---

## 🔄 方案二：使用 npm 代替 pnpm（最简单，不需要安装新工具）

如果您不想安装 pnpm，可以直接使用 npm！

### pnpm 命令 vs npm 命令对照表

| pnpm 命令 | npm 命令 | 说明 |
|-----------|---------|------|
| `pnpm install` | `npm install` | 安装依赖 |
| `pnpm add <包名>` | `npm install <包名>` | 安装新包 |
| `pnpm add -D <包名>` | `npm install --save-dev <包名>` | 安装开发依赖 |
| `pnpm remove <包名>` | `npm uninstall <包名>` | 删除包 |
| `pnpm build` | `npm run build` | 构建项目 |
| `pnpm dev` | `npm run dev` | 开发模式运行 |
| `pnpm start` | `npm start` | 生产模式运行 |

### 使用 npm 的完整步骤

#### 步骤 1：检查 npm 是否已安装

```cmd
npm --version
```

如果显示版本号，可以直接使用 npm。

#### 步骤 2：安装项目依赖

```cmd
cd Desktop\Joe-checkin
npm install
```

这个命令会读取 `package.json`，安装所有依赖。

#### 步骤 3：构建项目

```cmd
npm run build
```

#### 步骤 4：运行开发服务器

```cmd
npm run dev
```

打开浏览器访问 `http://localhost:5000`。

---

## 🚀 快速开始：最简单的方法

### 如果 npm 已安装（推荐）

直接使用 npm，无需安装 pnpm：

```cmd
# 1. 进入项目文件夹
cd Desktop\Joe-checkin

# 2. 安装依赖
npm install

# 3. 运行项目
npm run dev
```

### 如果 npm 也没有

#### 步骤 1：安装 Node.js

1. 访问：https://nodejs.org
2. 下载 LTS 版本
3. 双击安装，全部点击 Next
4. 安装完成后重启 CMD

#### 步骤 2：使用 npm 运行项目

```cmd
cd Desktop\Joe-checkin
npm install
npm run dev
```

---

## ⚠️ 常见问题

### Q1：npm 也不是内部或外部命令

**原因**：没有安装 Node.js

**解决方法**：
1. 访问 https://nodejs.org
2. 下载并安装 Node.js（LTS 版本）
3. 重启 CMD

### Q2：安装 pnpm 时提示权限错误

**原因**：需要管理员权限

**解决方法**：
1. 右键点击 CMD
2. 选择"以管理员身份运行"
3. 再次执行 `npm install -g pnpm`

### Q3：安装 pnpm 后仍然提示"不是内部或外部命令"

**原因**：环境变量未更新

**解决方法**：

**方法 A**：重启 CMD（关闭后重新打开）

**方法 B**：重启电脑

**方法 C**：添加到环境变量（高级）
1. 右键"此电脑" → 属性
2. 高级系统设置 → 环境变量
3. 在"系统变量"中找到"Path"
4. 点击"编辑"
5. 添加：`C:\Users\你的用户名\AppData\Roaming\npm`
6. 确定并保存

### Q4：npm install 速度很慢

**解决方法**：使用淘宝镜像

```cmd
npm config set registry https://registry.npmmirror.com
```

然后再执行：
```cmd
npm install
```

### Q5：npm install 报错"找不到模块"

**原因**：可能需要清理缓存

**解决方法**：
```cmd
npm cache clean --force
npm install
```

---

## 📋 完整操作流程（推荐使用 npm）

### 前提条件检查

```cmd
# 检查 npm 是否已安装
npm --version

# 如果未安装，先安装 Node.js
# 下载地址：https://nodejs.org
```

### 安装依赖并运行

```cmd
# 1. 进入项目文件夹
cd Desktop\Joe-checkin

# 2. 安装依赖（第一次需要几分钟）
npm install

# 3. 构建项目
npm run build

# 4. 运行开发服务器
npm run dev

# 5. 打开浏览器访问 http://localhost:5000
```

### 如果需要部署到 Vercel

请参考 [GITHUB_DEPLOY.md](GITHUB_DEPLOY.md) 或 [QUICK_DEPLOY.md](QUICK_DEPLOY.md)，使用 npm 代替 pnpm：

```cmd
# 初始化 Git 仓库
git init
git add .
git commit -m "Initial commit"

# 连接 GitHub 仓库
git remote add origin https://github.com/your-username/Joe-checkin.git
git branch -M main
git push -u origin main

# 然后在 Vercel 网页上导入即可
```

---

## 🎯 推荐方案

### 最简单：直接用 npm

✅ **优点**：
- Node.js 自带 npm，无需额外安装
- 命令简单，容易记忆
- 对初学者更友好

**操作**：
```cmd
npm install
npm run dev
```

### 专业：安装 pnpm

✅ **优点**：
- 更快的安装速度
- 更节省磁盘空间
- 更严格的依赖管理

**操作**：
```cmd
npm install -g pnpm
pnpm install
pnpm dev
```

---

## 📝 快速参考：npm vs pnpm 命令对照

### 日常使用

| 操作 | npm | pnpm |
|-----|-----|------|
| 安装依赖 | `npm install` | `pnpm install` |
| 安装新包 | `npm install <包名>` | `pnpm add <包名>` |
| 删除包 | `npm uninstall <包名>` | `pnpm remove <包名>` |
| 构建项目 | `npm run build` | `pnpm build` |
| 开发模式 | `npm run dev` | `pnpm dev` |
| 生产模式 | `npm start` | `pnpm start` |

### 全局安装

| 操作 | npm | pnpm |
|-----|-----|------|
| 安装全局包 | `npm install -g <包名>` | `pnpm add -g <包名>` |
| 查看全局包 | `npm list -g --depth=0` | `pnpm list -g` |

---

## 💡 小贴士

1. **重启 CMD**：安装 Node.js 或 pnpm 后，一定要重启 CMD 才能生效
2. **管理员权限**：如果提示权限错误，右键 CMD 选择"以管理员身份运行"
3. **使用淘宝镜像**：如果 npm 安装很慢，使用 `npm config set registry https://registry.npmmirror.com`
4. **清理缓存**：如果遇到问题，执行 `npm cache clean --force`
5. **查看错误**：如果 npm install 报错，查看完整的错误信息，通常是网络问题或缺少依赖

---

## 🎉 总结

### 您现在有两个选择

**选择 1：最简单（推荐）**
- 使用 npm（Node.js 自带）
- 无需安装额外工具
- 命令：`npm install` → `npm run dev`

**选择 2：更专业**
- 安装 pnpm
- 命令：`npm install -g pnpm` → `pnpm install` → `pnpm dev`

### 立即开始

1. 检查 npm：`npm --version`
2. 进入项目：`cd Desktop\Joe-checkin`
3. 安装依赖：`npm install`
4. 运行项目：`npm run dev`

就这么简单！🚀

---

## 📚 相关文档

- [DOWNLOAD_GUIDE.md](DOWNLOAD_GUIDE.md) - 下载和解压指南
- [GITHUB_DEPLOY.md](GITHUB_DEPLOY.md) - GitHub 部署教程
- [QUICK_DEPLOY.md](QUICK_DEPLOY.md) - 快速部署指南

如有问题，请查看相关文档或告诉我具体错误信息！
